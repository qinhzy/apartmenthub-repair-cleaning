spring:
  datasource:
    url: jdbc:h2:mem:apartmenthub;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true

server:
  port: 8080

